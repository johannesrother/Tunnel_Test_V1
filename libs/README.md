# CDN library boundary

This project intentionally does not vendor a JavaScript runtime or require a package manager. `index.html` loads Babylon.js directly from the official Babylon CDN. Keep this directory for any future static, redistributable browser libraries that must be self-hosted.
